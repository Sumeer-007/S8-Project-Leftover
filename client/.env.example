# API base (optional; defaults to http://localhost:8000)
# VITE_API_BASE=http://localhost:8000

# Web Push / FCM: Firebase Console → Project settings → Cloud Messaging → Web Push certificates → Key pair
# Without this, getFcmToken() fails and volunteers never get push.
VITE_FIREBASE_VAPID_KEY=your-web-push-key-pair-here
